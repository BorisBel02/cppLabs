//
// Created by Boris on 12.12.2021.
//

/*
#include "BlockException.h"

template<typename T> BlockException<T>::BlockException(const char* msg, T blc, argString inData){
    printMessage(msg, blc, inData);
}

template<typename T> void BlockException<T>::printMessage(const char* msg, T erb, argString inData){
    std::cout << msg << std::endl;
    std::cout << "Block type: " << decltype(*erb)() << " - " << erb->getType << std::endl;
    std::cout << "Input data: ";
    if(inData.size() > 1) {
        for (unsigned i = 1; i < inData.size(); ++i) {
            std::cout << "argument №" << i << " = " << inData[i] << ' ';
        }
        std::cout << std::endl;
    }
    else{
        std::cout << "~NONE~" << std::endl;
    }
}
*/
